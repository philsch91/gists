# gcc

```
sudo apt update
sudo apt install software-properties-common
sudo add-apt-repository [-y] ppa:ubuntu-toolchain-r/test # sudo add-apt-repository ppa:ubuntu-toolchain-r/ppa -y
sudo apt update
sudo apt install gcc-13 g++-13 # sudo apt install build-essential
sudo apt purge gcc-13 g++-13
```
